{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "tsc: build",
            "type": "shell",
            "command": "npx tsc --sourceMap",
            "group": "build",
            "problemMatcher": ["$tsc"],
            "presentation": {
                "reveal": "silent"
            }
        }
    ]
}
