# HOW TO .TS DEBUG

`tsc --sourceMap <my .ts file>`

then you can debug the typescript file with node

or simply

`tsc --init` to setup tsconfig.json

-   run +debug => create config => node.js

never mind...
